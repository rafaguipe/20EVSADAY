{
  "name": "evsaday",
  "version": "1.0.0",
  "description": "Site gamificado para registro de Estados Vibracionais (EVs)",
  "main": "client/src/index.js",
  "scripts": {
    "dev": "cd client && npm start",
    "server": "echo 'Server não é mais necessário - usando Supabase'",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "build:server": "echo 'Server não é mais necessário'",
    "build:all": "npm run build",
    "install-all": "npm install && cd client && npm install",
    "start": "cd client && npm start",
    "vercel-build": "npm run install-all && npm run build"
  },
  "keywords": [
    "conscienciologia",
    "ev",
    "gamification",
    "pixel-art"
  ],
  "author": "EVSADAY Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.52.0",
    "nodemailer": "^7.0.5",
    "react-hot-toast": "^2.5.2",
    "react-scripts": "^5.0.1"
  }
}
